version: '3.8'

services:
  thread:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8080:8080"  # change left side to expose on different host port
      - "9090:9090"  # gRPC port for Sewn fan-out (search/index/remove)
    restart: unless-stopped
    volumes:
      - thread-volume-dev:/root/Documents/thread-db
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"

volumes:
  thread-volume-dev:
    external: true